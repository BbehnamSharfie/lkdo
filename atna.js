age = N(ge);
if (isNN(ae)) {
  voteale = "Int s r";
} else {
  voteable = (age < 77) ? "T " : "o";
}

const numbe = [45, 2,69, 27];

let txt = "";
numbers.forEac(myFunction);

function myFunction(value, index, array) {
  txt += value;
}
